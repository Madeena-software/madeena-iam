name: "Download Database Backup"

on:
  workflow_dispatch:
    inputs:
      action:
        description: "What do you want to do?"
        required: true
        type: choice
        options:
          - "Download Latest"
          - "Download Specific"
          - "List Available Backups"
        default: "Download Latest"
      backup_name:
        description: "Filename (Only required if using 'Download Specific')"
        required: false
        type: string

concurrency:
  group: "{{APP_NAME}}-download-backup"
  cancel-in-progress: false

jobs:
  download-backup:
    name: Retrieve Backup
    runs-on: self-hosted
    
    steps:
      - name: Process Backups
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MINIO_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MINIO_SECRET_ACCESS_KEY }}
          AWS_BUCKET: ${{ secrets.MINIO_BUCKET }}
          AWS_ENDPOINT: ${{ secrets.MINIO_ENDPOINT }}
          APP_SLUG: "{{APP_NAME}}"
        run: |
          set -euo pipefail
          
          # Ensure workspace is clean before starting
          rm -f *.sql.gz
          
          ACTION="${{ github.event.inputs.action }}"
          BACKUP_INPUT="${{ github.event.inputs.backup_name }}"
          
          if [ "$ACTION" = "List Available Backups" ]; then
            echo "=== Available backups in S3 ==="
            docker run --rm \
              --entrypoint sh \
              -e AWS_ACCESS_KEY_ID \
              -e AWS_SECRET_ACCESS_KEY \
              -e AWS_ENDPOINT \
              minio/mc:latest -c "
                mc alias set s3 \"\$AWS_ENDPOINT\" \"\$AWS_ACCESS_KEY_ID\" \"\$AWS_SECRET_ACCESS_KEY\" --api S3v4 >/dev/null && \
                mc ls \"s3/$AWS_BUCKET/${APP_SLUG}-backups/\"
              " | grep '\.sql\.gz$' || echo "No backups found yet."
            echo "==============================="
            exit 0
          fi
          
          if [ "$ACTION" = "Download Specific" ]; then
            if [ -z "$BACKUP_INPUT" ]; then
              echo "ERROR: You selected 'Download Specific' but did not provide a filename."
              exit 1
            fi
            TARGET_FILE="$BACKUP_INPUT"
          else
            echo "Finding the latest backup..."
            TARGET_FILE=$(docker run --rm \
              --entrypoint sh \
              -e AWS_ACCESS_KEY_ID \
              -e AWS_SECRET_ACCESS_KEY \
              -e AWS_ENDPOINT \
              minio/mc:latest -c "
                mc alias set s3 \"\$AWS_ENDPOINT\" \"\$AWS_ACCESS_KEY_ID\" \"\$AWS_SECRET_ACCESS_KEY\" --api S3v4 >/dev/null && \
                mc ls \"s3/$AWS_BUCKET/${APP_SLUG}-backups/\"
              " | grep '\.sql\.gz$' | tail -n 1 | awk '{print $NF}')
          fi
          
          if [ -z "$TARGET_FILE" ]; then
            echo "No backups found!"
            exit 1
          fi
          
          echo "Generating presigned URL for $TARGET_FILE..."
          
          # aws s3 presign guarantees the URL includes full IAM signature query parameters, avoiding AccessDenied.
          PRESIGNED_URL=$(docker run --rm \
            -e AWS_ACCESS_KEY_ID \
            -e AWS_SECRET_ACCESS_KEY \
            -e AWS_DEFAULT_REGION=us-east-1 \
            amazon/aws-cli s3 presign "s3://$AWS_BUCKET/${APP_SLUG}-backups/$TARGET_FILE" \
            --endpoint-url "$AWS_ENDPOINT" \
            --expires-in 3600)
            
          if [ -z "$PRESIGNED_URL" ]; then
             echo "Failed to generate presigned URL!"
             exit 1
          fi
          
          # GitHub Actions automatically masks secrets (like the AWS_ENDPOINT and AWS_BUCKET) with ***.
          # This corrupts the presigned URL if we print it directly.
          # To securely bypass the log masker without using artifacts or third-party shorteners, we Base64 encode it.
          B64_URL=$(echo -n "$PRESIGNED_URL" | base64 -w 0)

          echo "✅ Backup is ready for download!"
          
          echo "### 📦 Database Backup Ready" >> $GITHUB_STEP_SUMMARY
          echo "The database backup **$TARGET_FILE** is ready for download." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Due to GitHub Actions aggressively masking security keys, the URL is provided securely below." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Run this command in your terminal to securely download it:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "curl -o $TARGET_FILE \"\$(echo '$B64_URL' | base64 --decode)\"" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "*The presigned signature expires in 1 hour.*" >> $GITHUB_STEP_SUMMARY
